- loop over all lines and initialize global variables
- loop over all lines and save all methods (with accepted parameters)
- loop over all lines and check if the code is valid


Class that parses a single line (using regex) and returns the type of line



///
1 - while statement implement Compileable
2 - if statement implement Compileable
3 - method call implement Compileable
4 - assighment implement Compileable
5 - var dec implement Compileable
6 - global var dec implement Compileable
7 - method dec implement Compileable

// inferface Compileable {
    void compile(param 1..);

}
